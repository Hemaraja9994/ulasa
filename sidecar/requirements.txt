# ULASA local sidecar.
#
# Speech recognition alone needs only the first block. Translation adds roughly
# 1 GB of model weights and a large torch install; skip it if you only want ASR.

# --- always required ---
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9

# --- speech recognition (faster-whisper) ---
faster-whisper>=1.0.3

# --- translation gloss (IndicTrans2) — optional ---
transformers>=4.44
torch>=2.2
sentencepiece>=0.2.0

# --- transliteration — optional ---
indic-transliteration>=2.3.60
